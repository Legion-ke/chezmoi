return {
  -- Disable the status bar plugin
  { "nvim-lualine/lualine.nvim",   enabled = false },

  -- Optional: Disable the file path bar at the top (bufferline)
  -- if you want to rely purely on Tmux for tabs too
  { "akinsho/bufferline.nvim",     enabled = false },

  -- Disable the sidebar file tree (Neo-tree)
  { "nvim-neo-tree/neo-tree.nvim", enabled = false },
}
